import {Heading} from "@chakra-ui/react"

const PageNotFound = ()=>{
    return(
        <div>
            <Heading>
            Since this is an assignment, only one page is working, as everything is just for demo purposes.     
           </Heading>
        </div>
    )
};

export default PageNotFound;